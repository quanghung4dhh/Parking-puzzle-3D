{
  "name": "parking-puzzle-3d",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "validate-levels": "node --import tsx scripts/validate-levels.ts",
    "validate:levels": "npm run validate-levels"
  },
  "dependencies": {
    "three": "^0.165.0"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "tsx": "^4.22.4",
    "typescript": "^5.5.4",
    "vite": "^8.1.0",
    "vitest": "^4.1.9"
  }
}
